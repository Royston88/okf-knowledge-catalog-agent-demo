# LINK MANAGEMENT IS DELIBERATELY LEFT TO `link-concepts.ts`.
#
# !! THE INTEROP CLAIM IS SCOPED TO ENTRIES AND ASPECTS, NOT LINKS. !!
# Because `entryLinks:` is omitted below, an UNMODIFIED kcmd reconciles links
# with an UNFILTERED lookup and deletes every link it does not find locally.
# MEASURED against a scratch group: one `related` link, one pristine push,
# "No local match found for existing link to undefined. DELETING." -> 0 links.
# So: push this workspace with the PATCHED kcmd in `kcmd/`. A pristine binary
# is safe for entries and aspects and will destroy the 24 schema-join + 58
# related links in this dataset.
# `entryLinks:` is NOT declared here, and that is now the safe choice rather
# than the destructive one. kcmd's push used to reconcile EntryLinks
# unconditionally, so omitting `entryLinks:` meant an unfiltered lookup and it
# deleted every link the bundle did not describe — measured, it destroyed all 12
# scan-generated `schema-join` links in this dataset, silently. That block is now
# guarded (sync.ts, "A manifest that says nothing about links must not manage
# links"), so omitting it means kcmd leaves links ALONE.
# Declaring `entryLinks: [related]` instead makes every push delete our related
# links and `reconcileRelatedLinks` recreate them seconds later — correct end
# state, pointless churn, and a window where the catalog has no links at all.
# `kcmd push` unconditionally reconciles EntryLinks on every entry it touches
# (sync.ts ~425, no guard). It calls lookupEntryLinks with the types listed
# HERE as the filter — and when the list is absent the lookup is UNFILTERED, so
# every link the local bundle does not describe is deleted. Measured: a push
# with no `entryLinks:` destroyed all 12 scan-generated `schema-join` links in
# this dataset, silently.
# Listing `related` narrows the blast radius to the link type we own; the scan's
# schema-join links are then invisible to the reconciler and survive.
# LAYOUT: OKF — the staged tree is a kcmd-NATIVE OKF bundle.
#
# The shim used to emit kcmd's DOCUMENTS-layout stash (`catalogEntry:`), which
# an unmodified kcmd cannot consume: `type: BigQuery Table` is not a 3-part
# dotted Dataplex ref, so upstream's parse falls through to `generic`, and the
# publishing filter then compares `...generic` against the `entries:` allowlist
# below and drops every asset-backed concept WHILE REPORTING SUCCESS. The staged
# tree now uses `x-kcmd`, the stash `OkfLayout` reads, so an unmodified kcmd —
# and Google's own MCP server at src/tool/mcp.ts — can consume it.
#
# Two consequences of the switch, both deliberate:
#   - the staged root directory is `bundle/`, not `catalog/` (kcmd's
#     `rootDirForLayout`); the push scripts follow it automatically.
#   - `OkfLayout` synthesises a directory `index` entry per folder and
#     regenerates the `index.md` listings in `finalize()`, which is how the
#     recorded `index.md` gap closes. On an INGESTED scope it does neither,
#     because such a scope cannot hold custom entries.
#
# `OKF_STAGING_FORM=catalogEntry` reverts to the documents layout, which is the
# only form that works against a fork WITHOUT `OkfLayout` — i.e. every checkout
# of Google's repo, since `layouts/okf.ts` is fork-only.
scope: kb.${GOOGLE_CLOUD_PROJECT}.${GOOGLE_CLOUD_LOCATION}.${OKF_ENTRY_GROUP}
layout: okf
snapshot:
  entries:
    - dataplex-types.global.generic
  aspects:
    - dataplex-types.global.generic
    - dataplex-types.global.overview
    - ${GOOGLE_CLOUD_PROJECT}.${GOOGLE_CLOUD_LOCATION}.okf
publishing:
  entries:
    - dataplex-types.global.generic
  aspects:
    - dataplex-types.global.generic
    - dataplex-types.global.overview
    - ${GOOGLE_CLOUD_PROJECT}.${GOOGLE_CLOUD_LOCATION}.okf
